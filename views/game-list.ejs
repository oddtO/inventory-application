
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Add game</title>
        <link rel="stylesheet" href="/index.css">
    </head>
    <body>
	  	
        <%- include('_side-bar') %>
        <%- include('_top-bar') %>
	
        <main>
			
            <div class="dashboard">
                <div class="named-list">
                    <h2>Games</h2>
                    <ul>

                        <% gameList.forEach((game) => { %>
                        <%- include('_game-item', {game}) %>
                        <% }) %>
                    </ul>
                </div>
            </div>
        </main>
    </body>
</html>
